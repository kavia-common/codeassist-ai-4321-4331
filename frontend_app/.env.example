# Frontend environment variables
# Set this to the origin of your backend (must include scheme and port).
# For local dev with uvicorn on port 3001:
# REACT_APP_BACKEND_BASE_URL=http://localhost:3001
#
# For preview environments served over HTTPS, ensure the backend URL is also HTTPS:
# Example:
# REACT_APP_BACKEND_BASE_URL=https://vscode-internal-29601-beta.beta01.cloud.kavia.ai:3001

REACT_APP_BACKEND_BASE_URL=

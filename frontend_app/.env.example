# React app environment variables
# PUBLIC_INTERFACE
# REACT_APP_BACKEND_BASE_URL is used by the frontend to reach the backend.
# If your frontend is served via HTTPS, ensure this URL is also HTTPS to avoid mixed-content issues.
REACT_APP_BACKEND_BASE_URL=http://localhost:3001

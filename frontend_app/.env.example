# Frontend environment config
# Base URL for backend API; used by src/api.js and diagnostics
REACT_APP_BACKEND_BASE_URL=http://localhost:3001

# E2E test base URL for the frontend (Playwright)
# E2E_BASE_URL=http://localhost:3000

# E2E backend URL (Playwright intercepts/assumes this value when needed)
# BASE_BACKEND_URL=http://localhost:3001
